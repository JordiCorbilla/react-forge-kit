# Vite App Recipe

I use this when I want the full client-side pattern set: URL filters, deep links, TanStack Query, Zustand, notifications, and optional realtime events.

```sh
pnpm create vite my-app --template react-ts
```

Copy the packages or examples I need from `react-forge-kit`, then keep server state, URL state, UI state, and component state separate.

# 0001 Use TanStack Query For Server State

I use TanStack Query for API-owned data because it gives me caching, retries, cancellation, mutations, optimistic updates, polling, and invalidation without turning server state into local state.

# 0002 Use Zustand For UI State

I use Zustand for UI-only state that should not be fetched from or saved to the server: density, selection, shell preferences, theme preference, and recently viewed ids.
